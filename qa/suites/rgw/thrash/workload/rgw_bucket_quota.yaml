# Amazon/S3.pm (cpan) not available as an rpm
#os_type: ubuntu
tasks:
- exec:
    client.0:
      - cpan install Amazon::S3
- exec:
    client.0:
      - cp -R /root/perl5/lib/perl5/Amazon /usr/local/share/perl5
- workunit:
    clients:
      client.0:
        - rgw/s3_bucket_quota.pl
